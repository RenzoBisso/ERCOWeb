@model Sucursal
<style>
    footer {
        display: none !important;
    }

    #canal-wpp {
        display: none !important;
    }
</style>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-header bg-dark text-white p-4">
                    <h4 class="mb-0"><i class="bi bi-tag-fill me-2"></i>Nueva Sucursal</h4>
                    <p class="mb-0 small opacity-75">Registra una nueva sucursal en el sistema.</p>
                </div>
                <div class="card-body p-4">
                    <form asp-controller="Admin" asp-action="CargarSucursal" method="post" enctype="multipart/form-data">

                        <input type="hidden" asp-for="IdSucursal" />

                        <div class="mb-3">
                            <label asp-for="Nombre" class="form-label fw-bold">Nombre de la Sucursal</label>
                            <input asp-for="Nombre" class="form-control form-control-lg" placeholder="Ej: Lujan..." required>
                        </div>

                        <div class="mb-4">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" role="switch" id="estado" name="Estado" value="true" checked>
                                <label class="form-check-label fw-bold" for="estado">Sucursal Activa</label>
                            </div>
                        </div>


                        <div class="mb-3">
                            <label asp-for="Ubicacion" class="form-label fw-bold">Ubicacion</label>
                            <input asp-for="Ubicacion" class="form-control form-control-lg" placeholder="Url de maps..." required>
                        </div>


                        <div class="mb-4">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" role="switch" id="estado" name="Principal" value="true" checked>
                                <label class="form-check-label fw-bold" for="estado">Sucursal principal</label>
                            </div>
                        </div>

                        

                        <div class="mb-3">
                            <label asp-for="Latitud" class="form-label fw-bold">Latitud</label>
                            <input asp-for="Latitud" class="form-control form-control-lg" placeholder="" required>
                        </div>


                        <div class="mb-3">
                            <label asp-for="Longitud" class="form-label fw-bold">Longitud</label>
                            <input asp-for="Longitud" class="form-control form-control-lg" placeholder="" required>
                        </div>


                        <div class="d-flex justify-content-end gap-2 border-top pt-3">
                            <a asp-action="Categorias" class="btn btn-outline-secondary px-4 rounded-pill">Cancelar</a>
                            <button type="submit" class="btn btn-dark px-5 rounded-pill shadow-sm">
                                <i class="bi bi-check-lg me-2"></i>Guardar Sucursal
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>